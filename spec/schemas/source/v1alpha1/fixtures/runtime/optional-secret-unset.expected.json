{
  "rule_id": "SRC-SECRET-RESOLVE",
  "validation_layer": "runtime",
  "register": "not_applicable",
  "go_test": "planned",
  "truth_table": [
    { "required": true, "invoking_env": "unset", "runtime": "pre-launch rejection" },
    { "required": true, "invoking_env": "empty", "runtime": "rejection" },
    { "required": true, "invoking_env": "nonempty", "runtime": "inject" },
    { "required": false, "invoking_env": "unset", "runtime": "omit + redacted warning" },
    { "required": false, "invoking_env": "empty", "runtime": "omit + redacted warning" },
    { "required": false, "invoking_env": "nonempty", "runtime": "inject" }
  ],
  "notes": "Register validates binding shape only. This file is the expected Runtime outcome, not a Source Schema fixture."
}
